# Node-Secrets-Assignment
Repo A — Hard-coded secret demo

- Shows a secret value hard-coded into source (app.js).
- Endpoint /show-secret demonstrates usage.
- This is insecure because secrets embedded in code can be leaked.
